rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in users (accounts created by the admin) can read/write.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
