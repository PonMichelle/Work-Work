// ── Firebase connection config ───────────────────────────────────────────────
// Paste the values from your Firebase project here.
// (Firebase Console → Project settings ⚙ → "Your apps" → Web app → SDK setup → Config)
//
// These values are NOT secret — they only identify your project. Real security
// comes from Firebase Authentication + Firestore security rules.
//
// Replace every "PASTE_..." below with your actual values, then save.

export const firebaseConfig = {
  apiKey: "PASTE_API_KEY",
  authDomain: "PASTE_PROJECT_ID.firebaseapp.com",
  projectId: "PASTE_PROJECT_ID",
  storageBucket: "PASTE_PROJECT_ID.appspot.com",
  messagingSenderId: "PASTE_SENDER_ID",
  appId: "PASTE_APP_ID",
};
